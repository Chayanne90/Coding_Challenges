package challenges_java;

public class stringChallenge1 {


    /* Challenge 1

    Given a string, take the last char and return a new string with the last char added at
    the front and back, so "cat" yields "tcatt". The original string will be length 1 or more.

    EXAMPLE:

    backAround("cat") → "tcatt"
    backAround("Hello") → "oHelloo"
    backAround("a") → "aaa"
    */



    //Complete the method: backAround
    public static String backAround(String str) {

        return str;
    }


    public static void main (String [] args) {

        String new_str = backAround("apple");

        System.out.println("The new String is: " + new_str);

    }
}
